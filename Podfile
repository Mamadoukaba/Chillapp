# Uncomment this line to define a global platform for your project
# platform :ios, '8.0'
use_frameworks!

pod 'SwiftyJSON'
pod 'THContactPicker'
pod 'Mixpanel

target “Chillapp” do
    pod "Mixpanel"
    pod "SwiftyJSON"
end
